# CPP_Linear_Search_Basic
This program demonstrates the basic implementation of Linear Search in C++. The user inputs array elements and a target value. The algorithm traverses the array sequentially and prints the index where the element is found.
