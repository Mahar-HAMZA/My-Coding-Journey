# SearchElement2DArray.java
This program searches for a specific element in a 2D array (matrix). If the element is found, it prints the element along with its position in the matrix. It demonstrates matrix traversal and searching logic in Java.
