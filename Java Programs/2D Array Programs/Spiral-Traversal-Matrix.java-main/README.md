# Spiral-Traversal-Matrix.java
This program prints the elements of a 2D array in spiral order using boundary control variables (top, bottom, left, right). It demonstrates advanced matrix traversal logic in Java.
